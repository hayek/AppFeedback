import SwiftUI

struct TasksSectionView: View {
    let repo: RepoConfig
    var inspector: ProjectInspectorModel

    var body: some View {
        Text("Tasks").foregroundStyle(.secondary)   // stub — replaced in U13
    }
}
